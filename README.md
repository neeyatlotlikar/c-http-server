# c-http-server
A minimal HTTP server in C that grows step-by-step into a modern, non-blocking, containerized system. The aim is to progressively build a real server that handles HTTP GET requests, serves files, uses non-blocking I/O for efficiency, and finally packages the whole thing into a lightweight Podman container.
